<div class="card">
    <div class = "card-body">
        <p class="fs-5"><span class="{{ $icon ?? 'fas fa-users' }}"></span> {{ $title ?? ''}}</p>
        <h2 class="fw-bold">{{ $data ?? 0}}</h2>
    </div>
</div>